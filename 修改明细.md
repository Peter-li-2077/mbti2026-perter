# MBTI 题库优化修改明细

## 修改日期
2025年

## 一、题目优化

### 1.1 文案优化
| 题目编号 | 原内容 | 修改后内容 |
|---------|-------|-----------|
| EI-6 | 想到什么说什么，边说边想/先在心里想清楚再说 | 边说边想，脱口而出/先构思完再表达 |
| EI-8 | 很轻松，能很快和人熟络起来/需要花时间，慢慢熟悉 | 轻松快速熟络新人/慢慢磨合才熟悉 |
| EI-15 | 告诉身边的人/自己先体会一下 | 第一时间分享趣事/独自回味感受 |
| SN-24 | 实用性和耐用性/独特风格和创意设计 | 看重实用耐久/偏爱创意造型 |
| SN-28 | 经典流行/实验小众 | 更关注歌词描述的现实场景与具体内容/更在意抽象意境与词曲创作思路 |
| SN-33 | 确定性和已知的事物/可能性和未知的事物 | 偏好确定事实/好奇未知可能 |
| TF-45 | 分析问题所在，给出建议/理解并共情对方的感受 | 理性拆解问题/优先共情安抚 |
| JP-53 | 提前规划好，按时间表一步步完成/先做起来，根据情况灵活调整 | 提前规划按表落地/先开工灵活改动 |
| JP-68 | 提前到达/经常踩点或迟到 | 尽量提前赴约/接受卡点、允许临时变动 |

### 1.2 删除重复题目（第一次迭代）
| 原题目编号 | 维度 | 删除原因 |
|-----------|-----|---------|
| TF-51 | TF | 与TF-37高度重复（规则vs人情） |
| JP-71 | JP | 与JP-60完全重复（确定性vs灵活性） |

### 1.3 新增题目（第二次迭代）
| 新增题目编号 | 维度 | 说明 |
|-------------|-----|------|
| EI-17 | EI | 补充精力恢复场景，有效提高信度，权重2 |
| JP-72 | JP | 补齐72总题，权重2 |

### 1.4 权重调整
| 题目编号 | 原权重 | 新权重 | 说明 |
|---------|-------|-------|-----|
| TF-45 | 1 | 2 | 应对负面情绪为TF关键场景，提升权重 |

### 1.5 题目数量变化
| 版本 | 题目数 | 说明 |
|-----|------|-----|
| 初始 | 72题 | - |
| 第一次迭代 | 70题 | 删除2道重复题 |
| 第二次迭代 | 72题 | 新增2道题目（EI-17和JP-72） |

当前题目分布：
- **EI维度**：17题（q1~q17）
- **SN维度**：18题（q18~q35）
- **TF维度**：16题（q36~q51）
- **JP维度**：21题（q52~q72）

## 二、代码文件修改

### 2.1 src/types.ts
**新增内容：**
```typescript
export interface ScoringResult {
  EI: { E: number; I: number; type: 'E' | 'I' | 'Neutral' };
  SN: { S: number; N: number; type: 'S' | 'N' | 'Neutral' };
  TF: { T: number; F: number; type: 'T' | 'F' | 'Neutral' };
  JP: { J: number; P: number; type: 'J' | 'P' | 'Neutral' };
}
```

### 2.2 src/utils/scoring.ts
**修改内容：**
- 新增 `calculateScoreWithNeutral` 函数
- 该函数使用中立阈值（维度总权重的10%）进行判断
- 输出 `ScoringResult` 接口格式
- 保留原有 `calculateScore` 函数向后兼容

**中立阈值逻辑：**
- 计算每个维度的总权重
- 设置中立阈值为总权重的10%
- 如果维度内两个倾向的得分差小于阈值，标记为 `Neutral`
- 否则标记为得分高的倾向

### 2.3 src/store/useStore.ts
**新增状态：**
- `shuffledQuestions: Question[]` - 打乱后的题目数组
- `questionShuffleMap: number[]` - 索引映射表，记录打乱后的索引与原始索引的关系

**新增方法：**
- `setShuffledQuestions(questions: Question[], shuffleMap: number[])` - 设置打乱后的题目和映射表
- 在 `resetTest` 中重置打乱相关状态

### 2.4 src/pages/Test.tsx
**新增功能：**
- 引入 `Fisher-Yates` 打乱算法
- 在组件初始化时自动打乱题目
- 使用打乱后的题目进行显示
- 保留原有答题流程

**打乱算法特点：**
- 真正的随机打乱，无偏置
- 同时打乱题目和记录索引映射
- 确保计分可以正确对应回原始题目

### 2.5 src/pages/Payment.tsx
**修改内容：**
- 添加 `getMappedAnswers` 函数
- 将打乱顺序的答案重新映射回原始题目顺序
- 确保计分逻辑使用正确的答案顺序

### 2.6 src/data/questions.ts
**主要修改内容：**

**第一次迭代（删除2道重复题）：**
- 删除了TF维度的第51题
- 删除了JP维度的第71题
- 重新编号剩余题目，确保q_id从1到70连续
- 调整了TF-45题的权重从1到2
- 更新了各题目的文案

**第二次迭代（新增2道题目，恢复72题）：**
- 新增EI维度第17题（q_id: 17）：长时间多人社交结束后，你一般？
  - A: 兴致不减，希望继续结伴活动（E，权重2）
  - B: 身心疲惫，想要独处休息（I，权重2）
- 新增JP维度第72题（q_id: 72）：临时插入额外工作，你一般？
  - A: 优先调整原有计划重新排布（J，权重2）
  - B: 顺势穿插，顺其自然推进（P，权重2）
- 重新调整所有题目编号，确保q_id从1到72连续

## 三、功能说明

### 3.1 题目打乱
- 每次开始测试时，题目顺序随机打乱
- 防止用户通过记忆顺序刻意答题
- 答案会正确映射回原始题目顺序进行计分

### 3.2 中立阈值
- 新的计分算法支持中立结果
- 当某个维度的两个倾向得分差小于该维度总权重的10%时，标记为Neutral
- 原有计分逻辑保持不变，向后兼容

## 四、文件清单

### 修改的文件
1. `src/types.ts`
2. `src/utils/scoring.ts`
3. `src/store/useStore.ts`
4. `src/pages/Test.tsx`
5. `src/pages/Payment.tsx`
6. `src/data/questions.ts`
7. `修改明细.md`

### 新增的文件
无

### 删除的文件
无

## 五、后续优化建议

1. **数据收集与分析**
   - 收集用户答题数据
   - 分析题目区分度
   - 计算Cronbach's α系数评估信度

2. **校验题**
   - 在适当位置插入校验题
   - 检测答题质量
   - 过滤无效答卷

3. **用户反馈**
   - 添加结果反馈功能
   - 收集用户对测试准确性的评价
   - 持续优化题目库

4. **UI/UX优化**
   - 优化答题进度显示
   - 添加更好的结果可视化
   - 优化移动端体验
